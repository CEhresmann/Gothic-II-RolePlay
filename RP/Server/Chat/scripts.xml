<server>
    <!-- 
        Chat Module Refactored
        - Service: Handles chat logic and state.
        - Commands: Modular command files are loaded by the controller.
        - Controller: Binds everything together.
    -->
    <script src="ChatController.nut" type="server" />
</server>
